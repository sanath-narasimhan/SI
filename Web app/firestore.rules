rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEBUG RULE: Allow unauthenticated reads
    // WARNING: Do not use in production
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
